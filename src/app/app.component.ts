import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: `<app-navigation-bar></app-navigation-bar>`, // Good idea but why only the nav-bar. Where is your page?
  //templateUrl: './app.component.html',
})
export class AppComponent {

}
